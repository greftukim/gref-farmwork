# 인수인계 문서 — 2026-04-09

## 오늘 완료한 작업

### 작업 0-6: RLS 전면 재설계 완료
- 16개 테이블 전부 RLS 보호 상태로 전환
- 9개 마이그레이션 파일 + 9개 롤백 파일 작성 및 프로덕션 DB 적용
- RLS 헬퍼 함수 8개 설치 (SECURITY DEFINER + STABLE + search_path 고정)
- 적용 상세: docs/rls-design.md "적용 완료 (2026-04-09)" 섹션 참조

### loginWithDeviceToken select 컬럼 축소 (RLS-DEBT-001 완화)
- src/stores/authStore.js: `select('*')` → 필수 11개 컬럼만 명시
- 제거된 필드: pin_code, phone, hire_date, annual_leave_days, emp_no,
  work_hours_per_week, password, created_at, username

### overtime_requests Realtime publication 수동 등록
- 마이그레이션 적용 중 overtime_requests가 supabase_realtime에 미등록 상태임을 발견
- Supabase SQL Editor에서 수동으로 추가

---

## 새로 추가된 RLS 백로그

docs/rls-design.md 부록 A 참조.

| 항목 | 내용 | 상태 |
|------|------|------|
| RLS-DEBT-010 | anon INSERT 시 worker_id 본인 검증 부재 | 완화됨 (EXISTS 검증) |
| RLS-DEBT-011 | 작업자 간 task 교차 수정 가능 | 부분 완화 (status IN 화이트리스트) |
| RLS-DEBT-012 | 작업자가 다른 작업자 미완료 출근 기록에 퇴근 대신 찍기 가능 | 부분 완화 (±1일+check_out IS NULL) |
| RLS-DEBT-013 | 작업자가 다른 worker_id로 휴가/연장 요청 제출 가능 | 완화됨 (관리자 승인 단계 발각) |
| RLS-DEBT-014 | attendance.date가 UTC 기준이라 "오늘" 판정 불가 | 완화됨 (±1일 범위) |

---

## 새로 발견된 UX 이슈 (RLS 무관)

| 항목 | 내용 |
|------|------|
| UX-001 | LoginPage 아이디 입력 시 @gref.local 자동 덧붙임으로 인한 혼란 |
| UX-002 | farm_admin 직원 등록 UI 부재 |
| UX-003 | farm_admin 직원 수정 UI 부재 |
| UX-004 | master 계정 엑셀 다운로드 기능 부재 |
| 기존 재확인 | farm_admin 연장근무 승인 UI 부재 |

---

## 다음 세션 우선순위 제안

1. **farm_admin 연장근무 승인 UI 구현** — 가장 즉시 필요. 재배팀이 연장근무 신청을 승인/시간조정할 수 없는 상태
2. **UX 백로그 정리 및 우선순위 결정** — UX-001~004 중 작업 순서 결정
3. **RLS-DEBT 앱 레벨 가드 추가** — RLS-DEBT-004 (deleteRecords master 가드), RLS-DEBT-007/008 (master 자기보호), RLS-DEBT-009 (auth_user_id 변경 방지)
4. **감사 로그 (작업 L) 또는 마스터 콘솔 (작업 M) 중 선택**

---

## 환경 변화 기록

- 단일 PC 작업으로 전환 확정 (`C:\Users\User\Desktop\gref-farmwork`)
- Claude 채팅(설계 자문)과 Claude Code(실행) 분업 체계 확립

---

## Git 상태

- 마지막 커밋: `6640cb9` — refactor: loginWithDeviceToken select 컬럼 축소
- 태그: `pre-rls-redesign` (e959ad9, 작업 전), `post-rls-redesign` (6640cb9, 완료)
